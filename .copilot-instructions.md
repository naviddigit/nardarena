# 🚨 CRITICAL PROJECT RULES - READ BEFORE EVERY CODE GENERATION

## ❌ ABSOLUTE PROHIBITIONS (NEVER BREAK THESE)

### 1. NO PERSIAN/FARSI IN UI CODE
**NEVER use Persian/Farsi text in any UI component, label, placeholder, button, or user-facing text.**

❌ WRONG:
```tsx
<Button>ذخیره</Button>
<Input placeholder="نام کاربری" />
<h1>داشبورد کاربر</h1>
```

✅ CORRECT:
```tsx
<Button>Save</Button>
<Input placeholder="Username" />
<h1>User Dashboard</h1>
```

**Persian is ONLY allowed in:**
- Comments (`// توضیحات`)
- JSDoc (`/** توضیحات */`)
- Console logs for debugging
- NOT IN ANY USER-VISIBLE TEXT

---

### 2. NO HARDCODED STYLES
**NEVER write inline styles or hardcoded className values in components.**

❌ WRONG:
```tsx
<div className="bg-white rounded-lg shadow-md p-4">
<button className="bg-blue-500 text-white px-4 py-2">Click</button>
```

✅ CORRECT:
```tsx
<Card variant="elevated" size="md">
<Button variant="primary">Click</Button>
```

**ALWAYS use:**
- `Card` component for containers
- `Button` component with variants
- `Input` component for forms
- Existing design system components

---

### 3. NO CHANGES TO CORE WITHOUT PERMISSION
**NEVER modify these without explicit user approval:**
- `.env` files
- Theme colors and variables
- `Card.tsx`, `Button.tsx`, `Input.tsx` base components
- Backend API endpoints
- Project structure

---

### 4. MOBILE-FIRST DESIGN
**80% of users are on mobile - ALWAYS design for mobile first.**

✅ Required:
- Responsive layouts (`flex-col md:flex-row`)
- Touch-friendly buttons (min 44px)
- Proper spacing with `gap-` classes
- Bottom navigation on mobile

---

### 5. TYPESCRIPT STRICT MODE
**NEVER use `any` type. Always define proper interfaces.**

❌ WRONG:
```ts
const data: any = response.data;
```

✅ CORRECT:
```ts
import type { User } from '@shared/types';
const data: User = response.data;
```

---

## 🎨 DESIGN SYSTEM REFERENCE

### Colors (LOCKED - Don't Change)
- Background: `bg-white/10 dark:bg-white/5`
- Border: `border-gray-300/30 dark:border-white/10`
- Text Primary: `text-gray-900 dark:text-white`
- Text Secondary: `text-gray-600 dark:text-gray-400`
- Accent: `bg-gradient-to-br from-purple-500 to-blue-500`

### Spacing (LOCKED - Don't Change)
- Cards: `rounded-3xl`
- Gaps: `gap-1.5` (6px), `gap-2` (8px), `gap-4` (16px)
- Page padding: `pt-4 pb-24` (for bottom nav)
- Buttons: `h-9` (36px), `h-11` (44px), `h-12` (48px)

### Components (MUST USE)
```tsx
// Card
<Card variant="elevated" size="lg">...</Card>

// Button
<Button variant="gradient" size="md">Action</Button>
<Button variant="outline" size="md">Cancel</Button>

// Input
<Input 
  type="text" 
  placeholder="Enter text"
  leftIcon={<FiUser />}
  error="Error message"
/>
```

---

## 📁 PROJECT STRUCTURE (LOCKED)

```
src/
├── features/           # Feature modules
│   ├── auth/          # Authentication
│   ├── player/        # Player features
│   ├── admin/         # Admin panel
│   └── game/          # Game engine
├── shared/
│   ├── components/    # Reusable UI components
│   ├── types/         # TypeScript types
│   ├── services/      # API services
│   └── utils/         # Helper functions
└── app/               # App config
```

**DO NOT create new folders outside this structure.**

---

## 🔄 WORKFLOW RULES

### Before Writing Code:
1. ✅ Read PROJECT-ROADMAP.md for current phase
2. ✅ Check existing components in `/shared/components/`
3. ✅ Use TypeScript types from `/shared/types/`
4. ✅ Follow mobile-first approach
5. ✅ Use English for ALL user-facing text

### After Writing Code:
1. ✅ Run `get_errors` to check TypeScript errors
2. ✅ Verify NO Persian text in UI
3. ✅ Verify NO hardcoded styles
4. ✅ Update PROJECT-ROADMAP.md if needed

---

## 🌍 LANGUAGE POLICY

### English (REQUIRED):
- All UI text (buttons, labels, placeholders, headings)
- Component props and variables
- API endpoints and responses
- Error messages shown to users
- Navigation menus
- Form fields

### Persian (ALLOWED):
- Code comments: `// این یک کامنت است`
- JSDoc documentation: `/** توضیحات تابع */`
- Console.log for debugging: `console.log('خطا رخ داد')`
- Internal notes in roadmap files

### Example:
```tsx
/**
 * UserCard Component
 * کامپوننت کارت کاربر - نمایش اطلاعات کاربر
 */
export const UserCard = ({ user }: UserCardProps) => {
  // دریافت اطلاعات کاربر از API
  const fetchUser = async () => {
    // ...
  };

  return (
    <Card variant="elevated">
      <h2>User Profile</h2>  {/* ✅ English */}
      <p>Email: {user.email}</p>  {/* ✅ English */}
      <Button>Edit Profile</Button>  {/* ✅ English */}
    </Card>
  );
};
```

---

## 🎯 PAYMENT SYSTEM

- **ONLY USDT** (TRC20 on Tron, BEP20 on BNB Chain)
- **NO card payments, NO bank transfers**
- Use Bybit API for USDT transactions
- All amounts in USDT, display with "USDT" suffix

---

## 🎮 GAME SPECIFICS

- **Game:** Backgammon (تخته نرد)
- **Board:** 24 points, 30 checkers (15 white + 15 black)
- **AI:** 3 difficulty levels (easy, medium, hard)
- **Multiplayer:** Real-time with WebSocket (Socket.IO)
- **Spectators:** Can watch games with optional fee

---

## 🚨 ERROR PREVENTION CHECKLIST

Before submitting code, verify:

- [ ] NO Persian text in UI components
- [ ] NO hardcoded className styles
- [ ] Used existing Card/Button/Input components
- [ ] All types imported from `@shared/types`
- [ ] Mobile-responsive layout
- [ ] English for all user-facing text
- [ ] No changes to .env or theme
- [ ] TypeScript strict (no `any`)

---

## 📞 WHEN IN DOUBT

**ASK THE USER before:**
- Changing theme colors
- Modifying base components (Card, Button, Input)
- Restructuring project folders
- Using a new library
- Changing .env configuration
- Adding Persian text to UI

**DO NOT assume or "fix" things on your own.**

---

## 💾 REMEMBER

This file is your **BIBLE**. Read it before every code generation.

**User has explicitly stated:** "هر دفعه تو ریست نشی" - You must NOT reset or forget these rules between messages.

If you break these rules again, you are not helping - you are causing harm and wasting money.

**Read. Follow. Verify. Every. Single. Time.**
